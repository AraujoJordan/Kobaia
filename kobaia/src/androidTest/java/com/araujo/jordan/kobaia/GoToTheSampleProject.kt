package com.araujo.jordan.kobaia

import androidx.test.internal.runner.junit4.AndroidJUnit4ClassRunner


import org.junit.Test
import org.junit.runner.RunWith

/**
 * As this is a test lib, it will make sense to test the tests in the sample app
 * Check the Sample from the sample project!
 */
@RunWith(AndroidJUnit4ClassRunner::class)
class GoToTheSampleProject {

    // Check the Sample from the sample project!
    @Test
    fun testTheLibFromTheSampleProjectTho() {
        assert(true)
    }
}
